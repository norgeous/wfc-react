// import { GridContainer, Row } from './styled';
// import Cell from './Cell';

const Grid = ({ grid }) => (
  <div>
    {/* {grid.map((row, y) => (
      <Row key={y}>
        {row.map((value, x) => (
          <Cell
            key={x}
            value={value}
            onClick={() => collapse(x,y)}
          />
        ))}
      </Row>
    ))} */}
    test
  </div>
);

export default Grid;
